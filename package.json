{
  "name": "hansup-backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc --noEmit false",
    "start": "node dist/src/index.js",
    "test": "npm run build && node --test dist/tests/*.test.js"
  },
  "dependencies": {
    "pg": "^8.12.0"
  },
  "devDependencies": {}
}
